# Set this to the directory containing your trained model artifacts.
MODEL_DIR=models
# Optional: point this to PostgreSQL in production. SQLite is used locally by default.
DATABASE_URL=sqlite:///data/agrivision.db
# Only enable this while designing the UI. Production endpoints must use trained models.
ALLOW_DEMO_RESPONSES=false
